{
  "name": "web-lgsm-frontend",
  "private": true,
  "description": "Build-time only. Bundles vendored JS (xterm.js, etc) into app/interface/static/js/vendor/. Not installed on end-user systems.",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
